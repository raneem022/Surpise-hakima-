<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎂 Happy Birthday Hakim!</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Poppins', sans-serif; background: white; color: black; }
        .container { max-width: 800px; margin: 50px auto; padding: 20px; text-align: center; }
        .date { font-size: 24px; margin: 20px 0; color: #333; }
        .tomorrow { font-size: 28px; color: #e91e63; margin: 20px 0; }
        .special { font-size: 32px; color: #2196f3; margin: 30px 0; }
        .hakim { font-family: 'Dancing Script', cursive; font-size: 48px; color: #e91e63; margin: 40px 0; }
        .btn { background: #4CAF50; color: white; border: none; padding: 15px 40px; font-size: 20px; border-radius: 50px; cursor: pointer; margin: 20px; }
        .photo { width: 300px; height: 300px; border-radius: 50%; margin: 30px auto; border: 10px solid gold; overflow: hidden; }
        .photo img { width: 100%; height: 100%; object-fit: cover; }
        .wish { font-size: 36px; color: #ff5722; margin: 30px 0; font-family: 'Dancing Script', cursive; }
        .hearts { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; }
        .heart { position: absolute; font-size: 30px; animation: fall 5s linear forwards; }
        @keyframes fall { to { transform: translateY(100vh) rotate(360deg); opacity: 0; } }
        .slide { display: none; }
        .active { display: block; animation: fadeIn 1s; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>
    <div class="container">
        <!-- Slide 1 -->
        <div class="slide active" id="slide1">
            <div class="date" id="today"></div>
            <div class="tomorrow" id="tomorrow"></div>
            <div class="special">Someone special is born!</div>
            <div class="hakim">Hakimaaaaaaa</div>
            <button class="btn" id="startBtn">🎁 Click Here 🎁</button>
        </div>
        
        <!-- Slide 2 -->
        <div class="slide" id="slide2">
            <div class="wish">Happy Birthday to the purest soul ever!</div>
            <div class="photo">
                <img src="https://i.postimg.cc/5HdR4NKw/photo.jpg" alt="Happy Birthday Hakim!">
            </div>
            <div class="hakim">Hakimaaaaaaa</div>
            <p style="margin-top: 30px; color: #666;">Made with ❤️ for an amazing friend</p>
        </div>
        
        <!-- Hearts Container -->
        <div class="hearts" id="hearts"></div>
    </div>

    <audio id="birthdayAudio" preload="auto">
        <source src="https://assets.mixkit.co/music/preview/mixkit-happy-birthday-to-you-443.mp3" type="audio/mpeg">
    </audio>

    <script>
        // Set dates
        const today = new Date();
        const tomorrow = new Date(today);
        tomorrow.setDate(tomorrow.getDate() + 1);
        
        document.getElementById('today').textContent = `Today: ${today.toDateString()}`;
        document.getElementById('tomorrow').textContent = `Tomorrow: ${tomorrow.toDateString()}`;
        
        // Elements
        const slide1 = document.getElementById('slide1');
        const slide2 = document.getElementById('slide2');
        const startBtn = document.getElementById('startBtn');
        const audio = document.getElementById('birthdayAudio');
        const hearts = document.getElementById('hearts');
        
        // Start celebration
        startBtn.addEventListener('click', function() {
            // Play audio for 10 seconds
            audio.play();
            setTimeout(() => audio.pause(), 10000);
            
            // Switch slides
            slide1.classList.remove('active');
            slide2.classList.add('active');
            
            // Create hearts
            createHearts();
            
            // Create sprinkles
            createSprinkles();
        });
        
        function createHearts() {
            const heartEmojis = ['❤️', '💖', '💕', '💗', '💓', '💞'];
            for(let i = 0; i < 50; i++) {
                setTimeout(() => {
                    const heart = document.createElement('div');
                    heart.className = 'heart';
                    heart.textContent = heartEmojis[Math.floor(Math.random() * heartEmojis.length)];
                    heart.style.left = Math.random() * 100 + 'vw';
                    heart.style.fontSize = (Math.random() * 30 + 20) + 'px';
                    heart.style.animationDuration = (Math.random() * 3 + 2) + 's';
                    hearts.appendChild(heart);
                    
                    // Remove after animation
                    setTimeout(() => heart.remove(), 5000);
                }, i * 100);
            }
        }
        
        function createSprinkles() {
            const emojis = ['🎉', '🎊', '🎈', '✨', '🌟'];
            for(let i = 0; i < 30; i++) {
                setTimeout(() => {
                    const sprinkle = document.createElement('div');
                    sprinkle.className = 'heart';
                    sprinkle.textContent = emojis[Math.floor(Math.random() * emojis.length)];
                    sprinkle.style.left = Math.random() * 100 + 'vw';
                    sprinkle.style.fontSize = (Math.random() * 25 + 15) + 'px';
                    sprinkle.style.animationDuration = (Math.random() * 2 + 1) + 's';
                    hearts.appendChild(sprinkle);
                    
                    setTimeout(() => sprinkle.remove(), 3000);
                }, i * 50);
            }
        }
        
        // Add some initial hearts
        createHearts();
    </script>
</body>
</html>